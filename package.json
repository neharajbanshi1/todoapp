{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "migrate:dev": "east migrate --es-modules",
    "rollback:dev": "east rollback --es-modules"
  },
  "dependencies": {
    "east": "^2.0.3",
    "nuxt": "^3.10.1",
    "postgres": "^3.4.3",
    "vue": "^3.4.15",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@nuxt/devtools": "^1.0.8"
  }
}
